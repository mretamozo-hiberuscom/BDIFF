"""Minimal entry point placeholder.

Proves the package imports and runs. No business logic (no DB connection,
schema discovery, comparison, or report rendering) belongs here yet — see
`docs/architecture/technical-baseline.md` for the planned architecture.
"""

from __future__ import annotations


def main() -> None:
    """Print a placeholder message and exit cleanly."""
    print("schema_comparator: scaffold placeholder - no TUI wired up yet.")


if __name__ == "__main__":
    main()
