# Processed References

No raw documents were supplied during `sdd-foundation` (see
`docs/references/raw/README.md`), so there are no processed summaries yet.

When a raw document is added later, its processed summary here MUST
include: source file, useful facts extracted, noise removed, assumptions
made, and any open questions the document raised.
